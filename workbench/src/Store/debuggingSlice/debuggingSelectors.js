// debugging slice selectors
export const selectSelectedTraceEntryIndex = (state) => state.debugging.selectedTraceEntryIndex;
export const selectSelectedTraceId = (state) => state.app.selectedTraceId;
